import LangUtil from '../utils/lang-util';
// const langUtil = new LangUtil();
export default {
    transModel(model, labels) {
        // if (model) {
        //     return langUtil.isEnglish() ? model[labels[0]] : model[labels[1]];
        // }
        // return '';
    }
}